<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="icon" type="image/png+xml" href="{{ asset('favicon.png') }}">

    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" 
          rel="stylesheet" 
          integrity="sha384-pzjw8f+ua7Kw1TIq0x3+v5c3dONpLqL7F4gTx1s5ZxwFhF5J7B3Q/mJdk74gbkvi" 
          crossorigin="anonymous">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    
    {% block stylesheets %}
        <style>
            /* Global Styles */
            body {
                background-color: #f8f9fa;
                font-family: 'Poppins', sans-serif;
                overflow-x: hidden;
            }

            /* Navbar */
            .navbar {
                position: sticky;
                top: 0;
                z-index: 1000;
                background-color: #000;
            }

            .navbar.sticky {
                background-color: #DF6951;
                box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            }

            .navbar-nav .nav-link {
                color: white !important;
            }

            .navbar-brand {
                color: white !important;
                font-weight: bold;
                display: flex;
                align-items: center;
            }

            .navbar-brand img {
                height: 40px; /* Ajuste la taille du logo */
                margin-right: 10px; /* Ajoute un petit espace entre le logo et le texte */
            }

            /* Mobile Navbar */
            @media (max-width: 991px) {
                .navbar {
                    background-color: black !important;
                }

                .navbar-nav .nav-link,
                .navbar-brand {
                    color: white !important;
                }

                .navbar-toggler {
                    border-color: white !important;
                }

                .navbar-toggler-icon {
                    filter: invert(1);
                }
            }

            /* Footer */
            .footer-section {
                background-color: #1a233a;
                color: #c3c7d1;
                padding: 40px 0;
            }

            .footer-title {
                color: #ffffff;
                font-size: 18px;
                margin-bottom: 20px;
                font-weight: bold;
            }

            .footer-links {
                list-style: none;
                padding: 0;
            }

            .footer-links li {
                margin-bottom: 10px;
            }

            .footer-links a {
                color: #c3c7d1;
                text-decoration: none;
                transition: 0.3s;
            }

            .footer-links a:hover {
                color: #D66A50;
            }

            .social-icons a {
                color: #ffffff;
                font-size: 20px;
                margin-right: 15px;
                transition: 0.3s;
            }

            .social-icons a:hover {
                color: #D66A50;
            }

            .footer-bottom {
                background-color: #161d2d;
                padding: 15px;
                margin-top: 20px;
                text-align: center;
            }
        </style>
    {% endblock %}
</head>
<body>
    {% block nav %}
    <nav id="navbar" class="navbar navbar-expand-lg navbar-light fixed-top">
      <div class="container">
        <a class="navbar-brand" href="{{ path('app_home') }}">
          <img src="{{ asset('logo.png') }}" alt="Logo"> WaW
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item">
              <a class="nav-link" href="{{ path('app_about') }}">A Propos</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{ path('app_voyage') }}">Voyages</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{ path('app_journal') }}">Carnet</a>
            </li>
            {% if app.user %}
              <li class="nav-item">
                <a class="nav-link" href="{{ path('app_profile') }}">Profil</a>
              </li>
            {% else %}
              <li class="nav-item">
                <a class="btn btn-outline-light" href="{{ path('app_login') }}">Se Connecter</a>
              </li>
              <li class="nav-item">
                <a class="btn btn-warning" href="{{ path('app_register') }}">S'inscrire</a>
              </li>
            {% endif %}
          </ul>
        </div>
      </div>
    </nav>
    {% endblock %}

    {% block body %}
        <!-- Contenu principal de la page -->
    {% endblock %}

    <!-- Footer -->
    <footer class="footer-section ">
        <div class="container">
            <div class="row">
                <!-- Section À propos -->
                <div class="col-md-4">
                    <h5 class="footer-title">À propos</h5>
                    <p>Notre plateforme vous permet de partager vos aventures de voyage, d'explorer des road trips et de vivre des expériences uniques.</p>
                </div>

                <!-- Section Liens Rapides -->
                <div class="col-md-4">
                    <h5 class="footer-title">Liens Rapides</h5>
                    <ul class="footer-links">
                        <li><a href="{{ path('app_home') }}">Accueil</a></li>
                        <li><a href="{{ path('app_dashboard_roadtrip_list') }}">Road Trips</a></li>
                        <li><a href="{{ path('app_register') }}">Inscription</a></li>
                    </ul>
                </div>

                <!-- Section Contact -->
                <div class="col-md-4">
                    <h5 class="footer-title">Nous Contacter</h5>
                    <p>Email : <a href="mailto:contact@roadtrip.com">contact@roadtrip.com</a></p>
                    <p>Téléphone : <a href="tel:+330123456789">+33 06 23 45 67 89</a></p>
                    <div class="social-icons">
                        <a href="#"><i class="fab fa-facebook"></i></a>
                        <a href="#"><i class="fab fa-twitter"></i></a>
                        <a href="#"><i class="fab fa-instagram"></i></a>
                    </div>
                </div>
            </div>
        </div>
        <div class="footer-bottom">
            <p>&copy; 2024 RoadTrip Explorer. Tous droits réservés.</p>
        </div>
    </footer>

    {% block javascripts %}
        <script>
          window.onscroll = function() {toggleStickyNavbar()};

          function toggleStickyNavbar() {
            let navbar = document.getElementById("navbar");
            if (window.pageYOffset > 50) {
              navbar.classList.add("sticky");
            } else {
              navbar.classList.remove("sticky");
            }
          }
        </script>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    {% endblock %}
</body>
</html>
