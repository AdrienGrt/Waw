<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Welcome!{% endblock %}</title>

    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" 
          rel="stylesheet" 
          integrity="sha384-pzjw8f+ua7Kw1TIq0x3+v5c3dONpLqL7F4gTx1s5ZxwFhF5J7B3Q/mJdk74gbkvi" 
          crossorigin="anonymous">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <!-- Icône -->
    <link rel="icon" 
          href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

    {% block stylesheets %}
        <style>
            /* Global Styles */
            body {
                background-color: #f8f9fa;
                font-family: 'Poppins', sans-serif;
                overflow-x: hidden;
            }

            /* Hero Section */
            .hero-section {
                background-image: url('https://images.unsplash.com/photo-1500964757637-c85e8a162699?w=800&auto=format&fit=crop&q=60&ixid=M3wxMjA3fDB8MHxzZWFyY2h8Nnx8bGFuZHNjYXBlfGVufDB8fDB8fHww');
                background-size: cover;
                background-position: center;
                position: relative;
                padding: 120px 0;
                color: #fff;
                text-align: center;
                overflow: hidden;
            }

            .hero-text h1 {
                font-size: 4rem;
                font-weight: 700;
                margin-bottom: 20px;
                animation: fadeInUp 1.5s ease-out;
                z-index: 1;
            }

            .hero-text p {
                font-size: 1.25rem;
                margin-bottom: 30px;
                animation: fadeInUp 2s ease-out;
                animation-delay: 0.5s;
                z-index: 1;
            }

            .btn-primary {
                background-color: #D66A50;
                border-color: #D66A50;
                padding: 12px 30px;
                font-size: 1.2rem;
                border-radius: 50px;
                box-shadow: 0 6px 10px rgba(0, 0, 0, 0.15);
                transition: 0.3s ease;
                z-index: 1;
            }

            .btn-primary:hover {
                background-color: #c15a42;
                border-color: #c15a42;
                box-shadow: 0 6px 15px rgba(0, 0, 0, 0.2);
            }

            /* Sticky Navbar */
            #navbar {
                transition: background-color 0.3s ease, box-shadow 0.3s ease;
                background-color: #000; /* Par défaut, la navbar est transparente */
            }

            #navbar.sticky {
                background-color: #DF6951; /* Fond orange quand on est en scroll */
                box-shadow: 0 4px 10px rgba(0,0,0,0.1); /* Ombre portée */
            }

            .navbar-nav .nav-link {
                color: white !important; /* Les éléments sont blancs */
            }

            .navbar-nav .btn {
                margin-left: 10px;
                border-radius: 50px;
            }

            .navbar-nav .btn-outline-light {
                color: white;
                border-color: white;
            }

            .navbar-nav .btn-outline-light:hover {
                background-color: white;
                color: #DF6951;
            }

            .navbar-nav .btn-warning {
                background-color: #DF6951;
                color: white;
            }
           
            .navbar-nav .btn-warning:hover {
                background-color: #DF6951;
            }

            /* Pour que la navbar soit sticky */
            .navbar {
                position: sticky;
                top: 0;
                z-index: 1000;
            }

            /* Custom color for WaW */
            .navbar-brand {
                color: white !important; /* WaW in white */
            }

            .footer {
                background-color: #2c3e50;
                color: #fff;
                padding: 3rem 0;
                text-align: center;
            }

            .footer h4 {
                font-size: 1.5rem;
                margin-bottom: 1.5rem;
            }

            .footer .btn-light {
                background-color: transparent;
                border: 2px solid #fff;
                color: #fff;
                padding: 0.5rem 1.5rem;
                border-radius: 50px;
                transition: all 0.3s ease;
            }

            .footer .btn-light:hover {
                background-color: #fff;
                color: #2c3e50;
            }

            /* Animations */
            @keyframes fadeInUp {
                0% {
                    opacity: 0;
                    transform: translateY(30px);
                }
                100% {
                    opacity: 1;
                    transform: translateY(0);
                }
            }

        </style>
    {% endblock %}
</head>
<body>
    {% block nav %}
    <nav id="navbar" class="navbar navbar-expand-lg navbar-light fixed-top">
      <div class="container">
        <a class="navbar-brand" href="{{ path('app_home') }}">WaW</a> <!-- Now the color is white -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item">
             <a class="nav-link" href="{{ path('app_about') }}">A Propos</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{ path('app_voyage') }}">Voyages</a> <!-- Route vers la liste des roadtrips -->
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{ path('app_journal') }}">Carnet</a>
            </li>

            {% if app.user %}
              <!-- Si l'utilisateur est connecté -->
              <li class="nav-item">
                <a class="nav-link" href="{{ path('app_profile') }}">Profil</a>
              </li>
            {% else %}
              <!-- Si l'utilisateur n'est pas connecté -->
              <li class="nav-item">
                <a class="btn btn-outline-light" href="{{ path('app_login') }}">Se Connecter</a>
              </li>
              <li class="nav-item">
                <a class="btn btn-warning" href="{{ path('app_register') }}">S'inscrire</a>
              </li>
            {% endif %}
          </ul>
        </div>
      </div>
    </nav>
    {% endblock %}

    {% block body %}
        <!-- Contenu principal de la page -->
    {% endblock %}

    {% block javascripts %}
        <script>
          // Ajouter ou enlever la classe "sticky" sur la navbar au scroll
          window.onscroll = function() {toggleStickyNavbar()};

          function toggleStickyNavbar() {
            let navbar = document.getElementById("navbar");
            if (window.pageYOffset > 50) {
              navbar.classList.add("sticky");
            } else {
              navbar.classList.remove("sticky");
            }
          }
        </script>

        <script>
            function initAutocomplete() {
                var departAddressInput = document.getElementById('address_depart');
                var arriveAddressInput = document.getElementById('address_arrive');

                var options = {
                    types: ['address'],
                    componentRestrictions: { country: 'FR' },  // Optionnel : restreindre à un pays spécifique
                    fields: ['place_id', 'geometry', 'name']
                };

                var autocompleteDepart = new google.maps.places.Autocomplete(departAddressInput, options);
                var autocompleteArrive = new google.maps.places.Autocomplete(arriveAddressInput, options);

                autocompleteDepart.addListener('place_changed', function() {
                    var place = autocompleteDepart.getPlace();
                    // Vous pouvez manipuler le lieu ici si nécessaire
                });

                autocompleteArrive.addListener('place_changed', function() {
                    var place = autocompleteArrive.getPlace();
                    // Vous pouvez manipuler le lieu ici si nécessaire
                });
            }
        </script>
        <script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyCMWOHqwyyuaA6tjdT3BC47LcKaa-rvbaY&libraries=places&callback=initAutocomplete" async defer></script>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
        <!-- jQuery -->
        <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" 
                integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zybcJfX4GdIY6cfF/JR8g2V8h9p3p7b25eb5dR65" 
                crossorigin="anonymous"></script>

        <!-- Popper.js -->
        <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.0.7/dist/umd/popper.min.js" 
                integrity="sha384-QnZz3L3QpBQd8i0r1w5/M5cr3EaaYTyzmLFmRaDGiEsXkQxoStcZtdJHT1T5K0GZ" 
                crossorigin="anonymous"></script>

        <!-- Bootstrap JS -->
        <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js" 
                integrity="sha384-pzjw8f+ua7Kw1TIq0x3+v5c3dONpLqL7F4gTx1s5ZxwFhF5J7B3Q/mJdk74gbkvi" 
                crossorigin="anonymous"></script>

        <!-- Test JavaScript -->
        <script>
            document.addEventListener('DOMContentLoaded', function () {
                console.log('Le JavaScript fonctionne bien !');
                // Exemple : change la couleur de fond du body
                document.body.style.backgroundColor = '#f8f9fa';
            });
        </script>
    {% endblock %}
</body>
</html>
